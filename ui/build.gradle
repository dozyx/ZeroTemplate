apply from: "${rootDir}/buildLib.gradle"

android {
    defaultConfig {
        versionCode 10000
        versionName "1.0.0"
    }
}

dependencies {
    testImplementation deps.junit
    api deps.appcompat
    api deps.constraint_layout
    api deps.material
}

