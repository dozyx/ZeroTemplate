apply from: "${rootDir}/buildLib.gradle"

android {
    defaultConfig {
        versionCode 10000
        versionName "1.0.0"
    }
}

dependencies {
    api Deps.appcompat
    api Deps.constraintLayout
    api Deps.material
}

