apply from: "${rootDir}/buildApp.gradle"
//apply from: "${rootDir}/buildAndResGuard.gradle"

dependencies {

    implementation project(":proguardtest:lib")

    implementation libs.appcompat
    implementation libs.material
    implementation project(':proguardtest:lib2')
//    compileOnly project(':proguardtest:lib2')
}